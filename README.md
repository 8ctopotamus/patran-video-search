# Patran Video Search

1. Once the plugin is installed, use the `[patran-video-search]` shortcode to add the search input to the page.

2. Add `data-tags="your, tags, here"` to any `li` tag containing a video that you want to be searchable. Tags should be separated by commas (,).
